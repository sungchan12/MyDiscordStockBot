# .env 템플릿. 이 파일을 복사해 `.env` 를 만들고 실제 값으로 채우세요:
#   cp .env.example .env
# .env 는 git 에 올라가지 않습니다.

# Discord 봇 토큰 (Developer Portal -> Bot -> Reset Token)
DISCORD_TOKEN=

# 메시지를 보낼 텍스트 채널 ID (개발자 모드 -> 채널 우클릭 -> ID 복사)
DISCORD_CHANNEL_ID=

# 종목 설정 파일 경로
STOCKS_FILE=stocks.yaml